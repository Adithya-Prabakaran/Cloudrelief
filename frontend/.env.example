# Backend API base URL. Point this at an AWS API Gateway / Oracle Cloud VM
# URL later — no other frontend file needs to change.
VITE_API_URL=https://4jz5lyekei.execute-api.ap-south-2.amazonaws.com/dev 
